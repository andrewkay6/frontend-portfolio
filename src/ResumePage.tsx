import SectionTitle from "./SectionTitle";

const ResumePage = () => {
    return (
        <div className="resume-container page">
            <SectionTitle
                text="Resume"
            />
        </div>
    );
};

export default ResumePage;